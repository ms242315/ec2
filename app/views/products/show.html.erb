<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @book.title %>
</p>

<p>
  <strong>Photo:</strong>
  <% if @book.photo.attached? %>
    <%= image_tag @book.photo %>
    <%= image_tag @book.thumbnail %>
  <% end %>
</p>

<p>
  <%= link_to '注文', new_order_path(id: @book.id) %>
</p>

